
docker compose up -d
docker ps
docker exec -it redis redis-cli ping
//docker exec -it kafka kafka-topics.sh --create --topic test-topic --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1

docker exec -it kafka bash -c "/usr/bin/kafka-topics --create --topic test-topic --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1"

docker compose restart

docker compose down
docker compose build --no-cache
docker compose up -d

docker compose up -d


mvn clean install -DskipTests
./mvnw spring-boot:run


